<h1 class="mt-2 text-center pt-4 text-light font-monospace">Informacion:</h1>
<div class="container w-50 mt-2 p-4 align-items-center">
<table class="table table-info">
  <tbody>
    {{#each info}}
    <tr>
      <th scope="row" >{{title}}</th>
      <td class="text-danger">{{description}}</td>
    </tr>
  {{/each}}
  </tbody>
</table>
</div>